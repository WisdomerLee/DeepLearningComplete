Deep Learning??

입력을 벡터로 받고 출력을 얻는 과정에서 layer를 두어 각각 계산을 통해 출력의 값을 계산하는 방식!
사용자가 내부 로직을 짜서 진행하는 것이 아닌 내부의 모델에서 layer의 계수를 자체 조정해가며 원하는 값을 얻는 상태를 만들 수 있는 것을 말함
hidden layer등이 있어 그 hidden layer의 내부 계수들을 아주 조금씩 조절하여 출력 값을 조정함
